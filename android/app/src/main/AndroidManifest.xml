<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!--
        In-app update (self-hosted distribution; there is no Play Store here). Lets the
        app hand the APK it downloaded from its own server to PackageInstaller. It does
        NOT install anything silently: Android still shows its own confirmation dialog,
        and the user must additionally have granted "install unknown apps" for this app,
        which `PackageManager.canRequestPackageInstalls()` is checked against first.
        Before that dialog the app verifies the download's SHA-256 against the digest
        the server published and that its signing certificate matches the running app's.
    -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />

    <application
        android:name=".PhosApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Phos"
        android:usesCleartextTraffic="true">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.Phos">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- AppAuth redirect handler -->
        <activity
            android:name="net.openid.appauth.RedirectUriReceiverActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="dev.phos.android" android:host="callback" />
            </intent-filter>
        </activity>

        <!--
            PackageInstaller session status. Not exported: the only sender is the
            platform, via a PendingIntent this app created, and an explicit action keeps
            an unrelated broadcast from being read as an install result.
        -->
        <receiver
            android:name=".update.InstallStatusReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="dev.phos.android.INSTALL_STATUS" />
            </intent-filter>
        </receiver>

        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="androidx.work.WorkManagerInitializer"
                android:value="androidx.startup"
                tools:node="remove" />
        </provider>

    </application>

</manifest>
