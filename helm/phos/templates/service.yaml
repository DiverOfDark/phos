apiVersion: v1
kind: Service
metadata:
  name: {{ include "phos.fullname" . }}
  labels:
    {{- include "phos.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "phos.selectorLabels" . | nindent 4 }}
